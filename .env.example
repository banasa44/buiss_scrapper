# =============================================================================
# Core Configuration
# =============================================================================

# Logging level (optional): debug, info, warn, error. Defaults to info.
LOG_LEVEL=info

# =============================================================================
# Database
# =============================================================================

# SQLite database file path (optional). Defaults to ./data/app.db
DB_PATH=./data/app.db

# =============================================================================
# Runner / Orchestration
# =============================================================================

# Execution mode (optional): once | forever. Defaults to once.
# - once: Run all queries once, then exit
# - forever: Run continuously in loop until terminated
RUN_MODE=once

# =============================================================================
# Provider: InfoJobs
# =============================================================================

# InfoJobs API credentials (required for runner execution)
IJ_CLIENT_ID=your_infojobs_client_id
IJ_CLIENT_SECRET=your_infojobs_client_secret

# =============================================================================
# Google Sheets Integration (Optional)
# =============================================================================

# Spreadsheet ID. When set, enables Sheets sync + feedback.
# Example: 1A2B3C4D5E6F7G8H9I0J
GOOGLE_SHEETS_SPREADSHEET_ID=

# Service account email (required when Sheets integration is enabled)
GOOGLE_SERVICE_ACCOUNT_EMAIL=service-account@your-project.iam.gserviceaccount.com

# Service account private key (required when Sheets integration is enabled)
# Use literal \n sequences for newlines in .env format
GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nREPLACE_ME\n-----END PRIVATE KEY-----\n"

# GCP project ID (optional, stored for completeness)
GOOGLE_PROJECT_ID=your-gcp-project-id

# =============================================================================
# Testing (Optional)
# =============================================================================

# Enable live Google Sheets connectivity tests (0 or 1)
# Requires valid Sheets credentials above
LIVE_SHEETS_TEST=0

